
# Private notes and helpers - never published.
# Study material stays local, never published.
*.bmp
*.bundle
*.dll
*.o
*.obj
*.xpl
FIXES.md
VRAM_INTELLIGENCE_MAP.md
VRAM_STUDY.md
build/
captures/
captures/*.rdc
dist/
learnings.md
patched_*.spv
resume-flight.ps1
stop-xp.ps1
tools/*.exe
tools/inject_test.exe
xp-shot.ps1
__pycache__/
<<<<<<< HEAD
=======
validation.txt

# Vendor upscaler SDKs. Not ours to redistribute - this project is
# GPL-3.0 and DLSS in particular ships under NVIDIA's own terms - and
# they are 450 MB of headers, import libraries and runtime DLLs that
# every clone would otherwise pay for. build.ps1 detects whatever is
# present and compiles the matching backend; absent means the backend
# reports no-library, which is a correct answer rather than an error.
third_party/
build/
dist/

# The FidelityFX SDK is a 424 MB upstream checkout, fetched not vendored.
third_party/FidelityFX-SDK/
>>>>>>> d2fcf7a (tools: generate FidelityFX shader permutations without MSVC)
